noarch_platforms:
  - linux_64
  # Windows is currently broken due to uvicorn-worker dependency.
  # Reenable once the problem is resolved.
  #- win_64
github:
  branch_name: main
  tooling_branch_name: main
conda_build:
  error_overlinking: true
conda_forge_output_validation: true
bot:
  automerge: true
